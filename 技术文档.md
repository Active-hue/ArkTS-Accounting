# 个人记账应用 - 技术文档

## 📱 项目简介

这是一款基于 HarmonyOS 开发的个人记账应用，提供账单管理、资产管理、数据统计等功能，帮助用户轻松管理个人财务。

---

## 🛠️ 技术栈

### 1. 开发语言与框架

#### **ArkTS (HarmonyOS 应用开发语言)**
- **版本**: 基于 TypeScript 扩展的声明式语言
- **SDK**: HarmonyOS SDK 5.0.5(17)
- **用途**: 应用核心开发语言

#### **声明式 UI 框架**
- **@Component**: 组件装饰器，定义可复用的 UI 组件
- **@Entry**: 页面入口装饰器
- **@State**: 状态管理装饰器，实现数据双向绑定
- **@Builder**: 自定义构建函数装饰器，用于构建可复用的 UI 片段

### 2. 数据持久化

#### **Preferences (轻量级数据存储)**
```typescript
import preferences from '@ohos.data.preferences';
```
- **用途**: 
  - 存储用户账号密码信息
  - 存储账户资产数据
- **特点**: 
  - 键值对存储
  - 适合小规模数据
  - 支持异步操作

#### **AppStorage (应用全局状态管理)**
```typescript
AppStorage.SetOrCreate('key', value);
AppStorage.Get<Type>('key');
```
- **用途**: 
  - 存储账单列表数据
  - 跨页面数据共享
- **特点**: 
  - 内存级存储
  - 应用生命周期内有效

### 3. 路由导航

#### **Router (页面路由)**
```typescript
import router from '@ohos.router';
```
- **用途**: 
  - 页面跳转和导航
  - 页面参数传递
- **主要方法**:
  - `router.pushUrl()`: 跳转到新页面
  - `router.replaceUrl()`: 替换当前页面
  - `router.back()`: 返回上一页

### 4. UI 组件库

#### **HarmonyOS 原生组件**
- **基础组件**: Column, Row, Stack, Text, Image, Button
- **容器组件**: List, Grid, Scroll
- **输入组件**: TextInput
- **图形组件**: Circle, Divider, Path
- **交互组件**: 自定义数字键盘、分类选择器

### 5. 系统能力

#### **Media Library Kit**
```typescript
import { photoAccessHelper } from '@kit.MediaLibraryKit';
```
- **用途**: 访问系统相册，用于用户头像上传
- **权限**: `ohos.permission.READ_IMAGEVIDEO`

#### **ArkUI Kit**
```typescript
import { promptAction } from '@kit.ArkUI';
```
- **用途**: 显示 Toast 提示信息

---

## 🏗️ 项目架构

### 目录结构

```
entry/src/main/
├── ets/
│   ├── common/              # 公共数据模型和管理类
│   │   ├── AccountData.ets  # 账户数据模型和管理
│   │   └── BillData.ets     # 账单数据模型和管理
│   ├── entryability/        # 应用入口能力
│   │   └── EntryAbility.ets
│   ├── pages/               # 页面文件
│   │   ├── Index.ets        # 登录/注册页面
│   │   ├── Main.ets         # 主页面（Tab导航）
│   │   ├── AddBill.ets      # 添加账单页面
│   │   ├── AddAccount.ets   # 添加账户页面
│   │   ├── Settings.ets     # 设置页面
│   │   ├── AboutUs.ets      # 关于我们
│   │   ├── HelpDocument.ets # 帮助文档
│   │   └── ...
│   └── entrybackupability/  # 数据备份能力
└── resources/               # 资源文件
    └── base/
        ├── element/         # 颜色、字符串、尺寸资源
        ├── media/           # 图片、图标资源
        └── profile/         # 配置文件
```

### 核心模块

#### **1. 用户认证模块**
- **文件**: `pages/Index.ets`
- **功能**: 
  - 用户登录（密码登录/验证码登录）
  - 用户注册
  - 账号信息持久化
- **数据存储**: Preferences

#### **2. 账单管理模块**
- **文件**: `pages/AddBill.ets`, `common/BillData.ets`
- **功能**:
  - 添加账单（支出/收入）
  - 分类管理（10种支出分类、10种收入分类）
  - 账单列表展示
  - 账单删除（左滑删除）
  - 按日期分组显示
- **数据存储**: AppStorage

#### **3. 资产管理模块**
- **文件**: `pages/AddAccount.ets`, `common/AccountData.ets`
- **功能**:
  - 添加账户（资金账户/信用账户）
  - 账户余额管理
  - 总资产/总负债计算
  - 账户删除
- **数据存储**: Preferences

#### **4. 数据统计模块**
- **文件**: `pages/Main.ets` (StatisticsPage)
- **功能**:
  - 月度支出/收入统计
  - 分类占比可视化（环形图）
  - 支出/收入榜单
  - 按年月筛选数据
- **可视化**: 使用 Path 组件绘制环形图

#### **5. 个人中心模块**
- **文件**: `pages/Main.ets` (ProfilePage)
- **功能**:
  - 头像上传
  - 昵称修改
  - 记账统计（记账天数、记账笔数）
  - 设置入口
  - 退出登录

---

## 💡 核心实现原理

### 1. 数据模型设计

#### **账单数据模型**
```typescript
interface BillItem {
  id: string;              // 唯一ID
  type: number;            // 0: 支出, 1: 收入
  amount: string;          // 金额
  category: string;        // 分类名称
  categoryIcon: string;    // 分类图标编号
  remark: string;          // 备注
  date: string;            // 日期 (YYYY-MM-DD)
  time: string;            // 时间 (HH:MM)
}
```

#### **账户数据模型**
```typescript
interface AccountItem {
  id: string;
  name: string;            // 账户名称
  type: string;            // 账户类型
  iconText: string;        // 图标文字
  iconColor: string;       // 图标颜色
  balance: string;         // 账户余额
  note: string;            // 备注
  currency: string;        // 币种
  includeInTotal: boolean; // 是否计入总资产
  canBeSelected: boolean;  // 记账时是否可被选择
  isCredit: boolean;       // 是否为信用账户
}
```

### 2. 数据管理类实现

#### **BillDataManager (账单数据管理)**
```typescript
export class BillDataManager {
  private static readonly STORAGE_KEY = 'bill_list';
  
  // 保存账单到 AppStorage
  static async saveBill(bill: BillItem): Promise<void>
  
  // 获取所有账单
  static async getAllBills(): Promise<BillItem[]>
  
  // 删除账单
  static async deleteBill(billId: string): Promise<boolean>
  
  // 计算统计数据
  static calculateStats(bills: BillItem[]): BillStats
}
```

#### **AccountDataManager (账户数据管理)**
```typescript
export class AccountDataManager {
  private static preferencesStore: preferences.Preferences | null = null;
  
  // 初始化存储
  static async init(context: Context): Promise<void>
  
  // 保存账户到 Preferences
  static async saveAccount(account: AccountItem): Promise<boolean>
  
  // 获取所有账户
  static async getAllAccounts(): Promise<AccountItem[]>
  
  // 删除账户
  static async deleteAccount(id: string): Promise<boolean>
  
  // 计算总资产和总负债
  static async calculateTotalAssets(): Promise<AssetsSummary>
}
```

### 3. 状态管理实现

#### **使用 @State 实现响应式更新**
```typescript
@Component
struct Main {
  @State billList: BillItem[] = [];      // 账单列表
  @State monthExpense: string = '0.00';  // 月度支出
  @State monthIncome: string = '0.00';   // 月度收入
  @State currentTabIndex: number = 0;    // 当前Tab索引
  @State refreshFlag: number = 0;        // 刷新标记
  
  // 数据变化时自动触发UI更新
  async loadBillData() {
    this.billList = await BillDataManager.getAllBills();
    // 计算统计数据...
    this.refreshFlag++; // 触发子组件刷新
  }
}
```

### 4. 页面导航实现

#### **底部Tab导航**
```typescript
@State currentTabIndex: number = 0; // 0:明细 1:统计 2:资产 3:我的

// 内容区域根据 Tab 索引显示不同页面
Stack() {
  if (this.currentTabIndex === 0) {
    this.BillPage();
  } else if (this.currentTabIndex === 1) {
    this.StatisticsPage();
  } // ...
}

// 底部导航栏点击切换
Column() {
  Image($r('app.media.bill_icon'))
  Text('明细')
}
.onClick(() => {
  this.currentTabIndex = 0;
})
```

### 5. 列表分组实现

#### **按日期分组账单**
```typescript
// 按日期分组
groupBillsByDate(bills: BillItem[]): Map<string, BillItem[]> {
  const grouped = new Map<string, BillItem[]>();
  bills.forEach(bill => {
    if (!grouped.has(bill.date)) {
      grouped.set(bill.date, []);
    }
    grouped.get(bill.date)?.push(bill);
  });
  return grouped;
}

// 渲染分组列表
List() {
  ForEach(this.dateGroups, (group: DateGroup) => {
    ListItem() {
      // 日期标题
      Text(group.date)
      // 该日期下的账单列表
      ForEach(group.bills, (bill: BillItem) => {
        // 账单项UI
      })
    }
  })
}
```

### 6. 环形图绘制实现

#### **使用 Path 绘制环形统计图**
```typescript
// 计算环形图路径
getPieSlicePath(percentage: number, startAngle: number): string {
  const centerX = 90;
  const centerY = 90;
  const outerRadius = 90;
  const innerRadius = 58; // 内圈半径
  
  const angle = (percentage / 100) * 360;
  const endAngle = startAngle + angle;
  
  // 转换为弧度
  const startRad = (startAngle - 90) * Math.PI / 180;
  const endRad = (endAngle - 90) * Math.PI / 180;
  
  // 计算外圆弧点
  const outerX1 = centerX + outerRadius * Math.cos(startRad);
  const outerY1 = centerY + outerRadius * Math.sin(startRad);
  const outerX2 = centerX + outerRadius * Math.cos(endRad);
  const outerY2 = centerY + outerRadius * Math.sin(endRad);
  
  // 计算内圆弧点
  const innerX1 = centerX + innerRadius * Math.cos(startRad);
  const innerY1 = centerY + innerRadius * Math.sin(startRad);
  const innerX2 = centerX + innerRadius * Math.cos(endRad);
  const innerY2 = centerY + innerRadius * Math.sin(endRad);
  
  const largeArc = angle > 180 ? 1 : 0;
  
  // 返回 SVG Path 命令
  return `M ${outerX1} ${outerY1} 
          A ${outerRadius} ${outerRadius} 0 ${largeArc} 1 ${outerX2} ${outerY2} 
          L ${innerX2} ${innerY2} 
          A ${innerRadius} ${innerRadius} 0 ${largeArc} 0 ${innerX1} ${innerY1} Z`;
}

// 绘制环形图
@Builder
PieSlice(item: CategoryStats, index: number) {
  Path()
    .width(180)
    .height(180)
    .commands(this.getPieSlicePath(item.percentage, this.getPieStartAngle(index)))
    .fill(item.color)
}
```

### 7. 自定义数字键盘

#### **键盘布局实现**
```typescript
@Builder
KeyboardArea() {
  Column() {
    // 金额显示区
    Text(this.amount || '0')
      .fontSize(60)
    
    // 数字键盘（4x4布局）
    Column() {
      Row() {
        this.NumberButton('7')
        this.NumberButton('8')
        this.NumberButton('9')
        this.NumberButton('今天')
      }
      Row() {
        this.NumberButton('4')
        this.NumberButton('5')
        this.NumberButton('6')
        this.NumberButton('+')
      }
      Row() {
        this.NumberButton('1')
        this.NumberButton('2')
        this.NumberButton('3')
        this.NumberButton('-')
      }
      Row() {
        this.NumberButton('.')
        this.NumberButton('0')
        this.DeleteButton()
        // 完成按钮
      }
    }
  }
}
```

### 8. 左滑删除实现

#### **使用 swipeAction 实现**
```typescript
ListItem() {
  // 账单项内容
  Row() {
    Image(bill.icon)
    Text(bill.category)
    Text(bill.amount)
  }
}
.swipeAction({
  end: this.DeleteButton(bill.id) // 右侧滑出删除按钮
})

@Builder
DeleteButton(billId: string) {
  Row() {
    Text('删除')
      .fontColor('#FFFFFF')
  }
  .width(80)
  .height('100%')
  .backgroundColor('#FF3B30')
  .onClick(() => {
    this.deleteBill(billId);
  })
}
```

### 9. 日期选择器实现

#### **自定义滚动选择器**
```typescript
@Builder
DatePickerDialog() {
  Stack() {
    // 半透明遮罩
    Column()
      .backgroundColor('rgba(0, 0, 0, 0.5)')
      .onClick(() => {
        this.showDatePicker = false;
      })
    
    // 选择器内容
    Column() {
      Row() {
        // 年份列表
        List({ initialIndex: this.yearList.indexOf(this.selectedYear) }) {
          ForEach(this.yearList, (year: number) => {
            ListItem() {
              Text(`${year}年`)
                .fontSize(this.selectedYear === year ? 20 : 16)
                .fontColor(this.selectedYear === year ? '#5B7FFF' : '#666666')
            }
            .onClick(() => {
              this.selectedYear = year;
            })
          })
        }
        
        // 月份列表
        List({ initialIndex: this.selectedMonth - 1 }) {
          ForEach(this.monthList, (month: number) => {
            ListItem() {
              Text(`${month}月`)
                .fontSize(this.selectedMonth === month ? 20 : 16)
            }
            .onClick(() => {
              this.selectedMonth = month;
            })
          })
        }
      }
      
      // 确定/取消按钮
      Row() {
        Button('取消')
        Button('确定')
          .onClick(() => {
            this.loadBillData(); // 重新加载数据
          })
      }
    }
  }
}
```

---

## 🎨 UI 设计特点

### 1. 设计风格
- **色彩主题**: 蓝色系 (#5B7FFF) 为主色调
- **风格**: 现代简约，卡片式设计
- **背景**: 浅蓝色渐变背景 (#E3ECFF)

### 2. 组件设计
- **圆角卡片**: 统一使用 12px 圆角
- **阴影效果**: 使用 shadow 增加层次感
- **渐变按钮**: 主要操作按钮使用渐变色
- **图标系统**: 20种分类图标（SVG格式）

### 3. 交互设计
- **反馈机制**: Toast 提示信息
- **动画效果**: 页面切换、列表滑动使用系统动画
- **手势操作**: 左滑删除、下拉刷新
- **键盘交互**: 自定义数字键盘，优化输入体验

---

## 🔧 核心技术难点与解决方案

### 1. 数据持久化选择

**问题**: 如何选择合适的数据存储方案？

**解决方案**:
- **Preferences**: 用于用户账号、账户信息等小规模结构化数据
- **AppStorage**: 用于账单列表等需要频繁读写的数据
- **优势**: 
  - Preferences 支持持久化，应用重启后数据不丢失
  - AppStorage 速度快，适合临时状态管理

### 2. 列表性能优化

**问题**: 大量账单数据时列表滚动卡顿

**解决方案**:
- 使用 `ForEach` 的第二个参数指定唯一 key，避免不必要的重绘
- 按日期分组，减少单个列表长度
- 使用 `List` 组件的懒加载特性

```typescript
ForEach(this.billList, 
  (bill: BillItem) => {
    // 渲染账单项
  }, 
  (bill: BillItem) => `${bill.id}_${this.refreshFlag}` // 唯一key
)
```

### 3. 状态更新与 UI 刷新

**问题**: 修改数组元素后 UI 不更新

**解决方案**:
- 使用 `refreshFlag` 强制刷新列表
- 创建新数组引用而不是修改原数组
- 增加刷新标记触发 key 变化

```typescript
// 删除账单后
this.billList = this.billList.filter(bill => bill.id !== deleteId);
this.refreshFlag++; // 触发刷新
```

### 4. 月度数据筛选

**问题**: 如何准确筛选指定月份的账单？

**解决方案**:
- 统一日期格式为 `YYYY-MM-DD`
- 使用字符串前缀匹配进行筛选

```typescript
const yearMonthStr = `${this.selectedYear}-${this.selectedMonth.toString().padStart(2, '0')}`;
const currentMonthBills = this.billList.filter(bill => 
  bill.date.startsWith(yearMonthStr)
);
```

### 5. 环形图绘制

**问题**: 如何绘制分类占比环形图？

**解决方案**:
- 使用 SVG Path 命令绘制环形扇区
- 计算累计起始角度，依次绘制各个扇区
- 使用三角函数计算圆弧上的点坐标

### 6. 跨页面数据同步

**问题**: 添加账单后返回主页，数据未更新

**解决方案**:
- 使用 `onPageShow` 生命周期函数
- 页面显示时重新加载数据

```typescript
onPageShow() {
  this.loadBillData();
  this.loadAccountData();
}
```

---

## 📊 数据流转图

```
用户操作
  ↓
页面组件 (@Component)
  ↓
状态变量 (@State)
  ↓
数据管理类 (BillDataManager / AccountDataManager)
  ↓
数据存储层 (AppStorage / Preferences)
  ↓
持久化存储
```

---

## 🚀 项目运行

### 环境要求
- **开发工具**: DevEco Studio
- **SDK**: HarmonyOS SDK 5.0.5(17)
- **设备**: HarmonyOS 手机/平板或模拟器

### 运行步骤
1. 使用 DevEco Studio 打开项目
2. 配置 HarmonyOS SDK
3. 连接 HarmonyOS 设备或启动模拟器
4. 点击运行按钮启动应用

### 测试账号
- **账号**: test
- **密码**: 123456

---

## 📝 功能列表

### ✅ 已实现功能
- [x] 用户登录/注册
- [x] 添加账单（支出/收入）
- [x] 账单列表展示（按日期分组）
- [x] 左滑删除账单
- [x] 月度收支统计
- [x] 分类占比可视化（环形图）
- [x] 支出/收入榜单
- [x] 按年月筛选数据
- [x] 添加资金账户
- [x] 总资产/总负债计算
- [x] 头像上传
- [x] 昵称修改
- [x] 记账统计（天数、笔数）

### 🔮 待完善功能
- [ ] 验证码登录
- [ ] 账单编辑功能
- [ ] 账户编辑功能
- [ ] 信用账户管理
- [ ] 借入/借出管理
- [ ] 数据导出功能
- [ ] 数据备份与恢复
- [ ] 记账提醒功能
- [ ] 预算管理
- [ ] 多账户切换
- [ ] 图表交互（点击查看详情）

---

## 🎯 项目特色

1. **纯原生开发**: 使用 HarmonyOS 原生框架，无第三方依赖
2. **声明式 UI**: 代码简洁，易于维护
3. **数据可视化**: 自定义环形图绘制，直观展示数据
4. **交互流畅**: 自定义数字键盘，左滑删除等交互
5. **响应式设计**: 使用 @State 实现数据双向绑定
6. **模块化设计**: 数据管理类与 UI 分离，便于扩展

---

## 📚 学习要点

### 适合学习的知识点
1. **ArkTS 语法**: 装饰器、状态管理、组件化开发
2. **声明式 UI**: Column, Row, List, Grid 等布局组件
3. **数据持久化**: Preferences 和 AppStorage 使用
4. **页面路由**: Router 页面导航
5. **自定义组件**: @Builder 构建可复用组件
6. **Canvas 绘图**: Path 绘制图形
7. **列表优化**: ForEach 性能优化技巧
8. **生命周期**: aboutToAppear, onPageShow 等

---

## 🐛 常见问题

### Q1: 数据持久化失败
**A**: 确保在使用 Preferences 前调用 `init()` 方法初始化存储

### Q2: UI 不更新
**A**: 检查状态变量是否使用 `@State` 装饰器，修改数组时创建新引用

### Q3: 路由跳转失败
**A**: 确保页面路径在 `main_pages.json` 中注册

### Q4: 图片资源加载失败
**A**: 使用 `$r('app.media.xxx')` 格式引用资源，确保资源文件存在

---

## 📧 联系方式

如有问题或建议，欢迎反馈！

---

## 📄 许可证

本项目仅供学习交流使用。

